# Capacitor 래퍼 — 로컬 생성물/비밀은 커밋 금지
node_modules/
android/                 # `npx cap add android`로 로컬 생성(대용량). 필요 시 일부만 추적.

# 서명 키스토어 — ⚠️ 절대 커밋 금지(분실 시 앱 업데이트 영구 불가). 별도 안전 백업.
*.jks
*.keystore
keystore.properties
